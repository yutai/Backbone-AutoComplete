$(document).ready(function() {
	/*
	var image_variations_template = $('#image_variations_template').html();
	var text_variations_template = $('#text_variations_template').html();
	var uploadForm = $('#new_variation');
	$(Mustache.to_html(text_variations_template,{})).appendTo(uploadForm);
	*/
});